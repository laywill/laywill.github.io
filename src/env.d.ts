// eslint-disable-next-line @typescript-eslint/triple-slash-reference -- Astro's ambient client types (ImportMetaEnv, asset imports, etc.) are only available via this triple-slash reference; there is no `import` equivalent.
/// <reference types="astro/client" />
